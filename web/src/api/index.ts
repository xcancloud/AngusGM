import { GM, STORE } from '@xcan-angus/tools';

import Tenant from '@/api/Tenant';
import User from '@/api/User';
import UserDirectory from '@/api/UserDirectory';
import Dept from '@/api/Dept';
import Group from '@/api/Group';
import OrgTag from '@/api/OrgTag';
import App from '@/api/App';
import Auth from '@/api/Auth';
import UserToken from '@/api/UserToken';
import SystemToken from '@/api/SystemToken';
import Service from '@/api/Service';
import Social from '@/api/Social';
import Online from '@/api/Online';
import Actuator from '@/api/Actuator';
import Email from '@/api/Email';
import Event from '@/api/Event';
import SystemLog from '@/api/SystemLog';
import UserLog from '@/api/UserLog';
import Message from '@/api/Message';
import Notice from '@/api/Notice';
import Setting from '@/api/Setting';
import Sms from '@/api/Sms';
import Edition from '@/api/Edition';
import Store from '@/api/Store';
import License from '@/api/License';

export const tenant = new Tenant(GM);
export const user = new User(GM);
export const userDirectory = new UserDirectory(GM);
export const dept = new Dept(GM);
export const group = new Group(GM);
export const orgTag = new OrgTag(GM);
export const app = new App(GM);
export const auth = new Auth(GM);
export const userToken = new UserToken(GM);
export const systemToken = new SystemToken(GM);
export const service = new Service(GM);
export const social = new Social(GM);
export const online = new Online(GM);
export const actuator = new Actuator();
export const email = new Email(GM);
export const event = new Event(GM);
export const systemLog = new SystemLog(GM);
export const userLog = new UserLog(GM);
export const message = new Message(GM);
export const notice = new Notice(GM);
export const setting = new Setting(GM);
export const sms = new Sms(GM);
export const edition = new Edition(STORE);
export const store = new Store(STORE);
export const license = new License(STORE);
