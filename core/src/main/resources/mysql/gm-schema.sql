--
-- Copyright (c) 2025   XCan Company
--
--        http://www.xcan.cloud
--
-- Licensed under the XCBL(XCan Business License) Version 1.0.
-- Detail XCBL license at:
--
-- http://www.xcan.cloud/licenses/XCBL-1.0
--

-- 1. Create and grant User（Execute via root account）
-- CREATE USER 'gm'@'%' IDENTIFIED BY '3Jsi1ss7Hvu';
-- grant all privileges on xcan_gm.* to gm@'%' identified by '3Jsi1ss7Hvu';
-- flush privileges;


-- 2. Create database（Execute via root account）
-- CREATE DATABASE `xcan_gm` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_bin ;

-- 3. Create table

-- 4. Drop all（Execute via root account）
-- DROP DATABASE IF EXISTS xcan_gm;
-- DROP USER IF EXISTS gm;
